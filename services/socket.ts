/*
import {io} from "socket.io-client";
const socket = io("http://192.168.1.107:8001", { transports : ['websocket'], upgrade: false })
//const socket = io("https://oddie-server.herokuapp.com/");


/!*const socket = io('https://oddie-server.azurewebsites.net', {
    withCredentials: true,
    forceNew: true,
    reconnectionAttempts: "Infinity", //avoid having user reconnect manually in order to prevent dead clients after a server restart
    timeout: 10000, //before connect_error and connect_timeout are emitted.
    transports: ['websocket']
});*!/
//const socket = io("ftps://waws-prod-db3-211.ftp.azurewebsites.windows.net/site/wwwroot")
//const socket = io("https://20.50.64.24/")


export {socket}*/
